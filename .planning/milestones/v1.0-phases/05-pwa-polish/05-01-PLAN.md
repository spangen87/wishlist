---
phase: 05-pwa-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/login/page.tsx
  - src/app/register/page.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Accent color is #F9A87A (peach) on all CTA buttons — vivid orange #F97316 is gone"
    - "layout.tsx title is 'Min önskelista', description is in Swedish, html lang='sv'"
    - "Open Graph og:title and og:description are set in layout metadata"
    - "globals.css --color-accent is #F9A87A and --color-accent-hover is #F49060"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Updated CSS custom properties for pastel accent"
      contains: "--color-accent: #F9A87A"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Swedish metadata and OpenGraph"
      contains: "Min önskelista"
    - path: "src/components/OfflineBanner.tsx"
      provides: "Offline detection client component"
      exports: ["OfflineBanner"]
    - path: "src/components/UpdateToast.tsx"
      provides: "SW update detection client component"
      exports: ["UpdateToast"]
  key_links:
    - from: "src/app/globals.css"
      to: "all pages"
      via: "CSS custom property --color-accent propagation"
      pattern: "--color-accent: #F9A87A"
    - from: "src/app/layout.tsx"
      to: "OfflineBanner + UpdateToast"
      via: "component import and render in RootLayout body"
      pattern: "OfflineBanner|UpdateToast"
---

<objective>
UI polish pass: update the CSS accent token to pastel peach, add Swedish metadata to the root layout, and create the OfflineBanner and UpdateToast client components that will be mounted globally.

Purpose: Deliver UI-01 (pastel child-friendly design). The accent token swap propagates automatically to all pages because the entire app already uses `--color-accent` via CSS custom properties. Two new system-level components provide offline and update awareness.

Output: Updated globals.css, updated layout.tsx (metadata + new components mounted), new OfflineBanner.tsx, new UpdateToast.tsx, updated offline fallback page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pwa-polish/05-CONTEXT.md
@.planning/phases/05-pwa-polish/05-UI-SPEC.md

<interfaces>
<!-- Key types and patterns the executor needs. -->

From src/app/globals.css (CURRENT STATE — must change):
```css
:root {
  --color-bg: #FFF9F5;
  --color-card: #FFF0E8;
  --color-accent: #F97316;        /* → REPLACE with #F9A87A */
  --color-accent-hover: #EA6C0A;  /* → REPLACE with #F49060 */
  --color-border: #E5D5CC;
  --color-text: #171717;
  --color-muted: #6B7280;
  --color-destructive: #DC2626;
}
```

From src/app/layout.tsx (CURRENT STATE — must change):
```typescript
export const metadata: Metadata = {
  title: "Create Next App",             /* → REPLACE */
  description: "Generated by create next app", /* → REPLACE */
};
// html lang="en"  → must change to "sv"
```

Target layout.tsx metadata:
```typescript
export const metadata: Metadata = {
  title: "Min önskelista",
  description: "Barnets önskelista — koordinera inköp utan att förstöra överraskningen",
  openGraph: {
    title: "Min önskelista",
    description: "Barnets önskelista — koordinera inköp utan att förstöra överraskningen",
  },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update CSS accent tokens and root layout metadata</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/app/offline/page.tsx</files>
  <read_first>
    - src/app/globals.css — current CSS custom properties (see current state in interfaces above)
    - src/app/layout.tsx — current metadata and html element (see current state in interfaces above)
    - .planning/phases/05-pwa-polish/05-UI-SPEC.md — full color table and copywriting contract
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/opengraph-image.md — if available, for OpenGraph metadata API shape
  </read_first>
  <action>
**1a — globals.css:** In the `:root` block, change exactly two lines:
- `--color-accent: #F97316;` → `--color-accent: #F9A87A;`
- `--color-accent-hover: #EA6C0A;` → `--color-accent-hover: #F49060;`
All other tokens remain unchanged (D-02: background tokens stay as-is).

**1b — layout.tsx:** Make these exact changes:
- Change `html lang="en"` to `html lang="sv"`
- Replace the `metadata` export with:
```typescript
export const metadata: Metadata = {
  title: "Min önskelista",
  description: "Barnets önskelista — koordinera inköp utan att förstöra överraskningen",
  openGraph: {
    title: "Min önskelista",
    description: "Barnets önskelista — koordinera inköp utan att förstöra överraskningen",
  },
};
```
Do NOT change the font imports, AuthProvider import, or the component structure — only the metadata export and lang attribute.

**1c — app/offline/page.tsx:** Create this file as a new app route at `src/app/offline/page.tsx`. This is the offline fallback page served by the service worker when navigation fails offline. Content per UI-SPEC:
```typescript
export default function OfflinePage() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-[#FFF9F5] px-4 text-center">
      <span aria-hidden="true" className="text-[64px] mb-6">📶</span>
      <h1 className="text-3xl font-semibold text-[#171717] mb-3">Du är offline</h1>
      <p className="text-base text-[#171717]">
        Kontrollera din internetanslutning och försök igen.
      </p>
    </div>
  );
}
```
No primary CTA needed (no action possible when offline).
  </action>
  <verify>
    <automated>grep -n "color-accent: #F9A87A" /Users/spangen87/Documents/GitHub/wishlist/src/app/globals.css && grep -n "color-accent-hover: #F49060" /Users/spangen87/Documents/GitHub/wishlist/src/app/globals.css && grep -n 'lang="sv"' /Users/spangen87/Documents/GitHub/wishlist/src/app/layout.tsx && grep -n "Min önskelista" /Users/spangen87/Documents/GitHub/wishlist/src/app/layout.tsx && test -f /Users/spangen87/Documents/GitHub/wishlist/src/app/offline/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` contains `--color-accent: #F9A87A;` (exact string, no trailing whitespace issues)
    - `globals.css` contains `--color-accent-hover: #F49060;`
    - `globals.css` does NOT contain `#F97316` or `#EA6C0A` anywhere
    - `layout.tsx` contains `lang="sv"`
    - `layout.tsx` contains `title: "Min önskelista"`
    - `layout.tsx` contains `openGraph:` block with title and description
    - `src/app/offline/page.tsx` exists
    - `offline/page.tsx` contains "Du är offline" heading text
  </acceptance_criteria>
  <done>Token swap complete (old vivid orange gone), Swedish metadata live, offline fallback page exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create OfflineBanner and UpdateToast client components</name>
  <files>src/components/OfflineBanner.tsx, src/components/UpdateToast.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/components/AuthProvider.tsx — established pattern for 'use client' root-level providers, to match import/export style
    - src/app/layout.tsx — where components must be mounted (read after Task 1 updates it)
    - .planning/phases/05-pwa-polish/05-UI-SPEC.md — OfflineBanner and UpdateToast component specs
    - .planning/phases/05-pwa-polish/05-RESEARCH.md — Pattern 3 (OfflineBanner) and Pattern 4 (UpdateToast) implementation patterns
  </read_first>
  <action>
**2a — OfflineBanner.tsx:** Create `src/components/OfflineBanner.tsx`:
```typescript
'use client';

import { useState, useEffect } from 'react';

export function OfflineBanner() {
  const [isOffline, setIsOffline] = useState(false);

  useEffect(() => {
    const handleOffline = () => setIsOffline(true);
    const handleOnline = () => setIsOffline(false);
    window.addEventListener('offline', handleOffline);
    window.addEventListener('online', handleOnline);
    setIsOffline(!navigator.onLine);
    return () => {
      window.removeEventListener('offline', handleOffline);
      window.removeEventListener('online', handleOnline);
    };
  }, []);

  return (
    <div
      role="status"
      aria-live="polite"
      className={`sticky top-0 z-50 flex items-center px-4 overflow-hidden transition-all duration-300 text-sm text-[#171717] ${
        isOffline ? 'opacity-100 h-11' : 'opacity-0 h-0'
      }`}
      style={{ background: '#FFF0E8', borderBottom: '1px solid #E5D5CC' }}
    >
      Du är offline — data kan vara föråldrad
    </div>
  );
}
```
Key requirements per UI-SPEC:
- `role="status"` + `aria-live="polite"` for screen reader announcement
- Fade transition via `transition-all duration-300` toggling opacity and height
- Background `#FFF0E8`, border-bottom `1px solid #E5D5CC` (warm neutral, not alarming)
- Sticky top-0, z-50
- Height minimum: h-11 (44px) when visible to meet touch target baseline

**2b — UpdateToast.tsx:** Create `src/components/UpdateToast.tsx`:
```typescript
'use client';

import { useState, useEffect } from 'react';

export function UpdateToast() {
  const [showUpdate, setShowUpdate] = useState(false);
  const [registration, setRegistration] = useState<ServiceWorkerRegistration | null>(null);
  const [updating, setUpdating] = useState(false);

  useEffect(() => {
    if (!('serviceWorker' in navigator)) return;

    navigator.serviceWorker.ready.then((reg) => {
      // Already waiting on mount
      if (reg.waiting) {
        setRegistration(reg);
        setShowUpdate(true);
      }
      // Detect new SW installing and transitioning to 'installed'
      reg.addEventListener('updatefound', () => {
        const newWorker = reg.installing;
        if (!newWorker) return;
        newWorker.addEventListener('statechange', () => {
          if (
            newWorker.state === 'installed' &&
            navigator.serviceWorker.controller
          ) {
            setRegistration(reg);
            setShowUpdate(true);
          }
        });
      });
    });
  }, []);

  const handleUpdate = () => {
    if (!registration?.waiting) return;
    setUpdating(true);
    registration.waiting.postMessage({ type: 'SKIP_WAITING' });
    window.location.reload();
  };

  if (!showUpdate) return null;

  return (
    <div
      role="status"
      aria-live="polite"
      className="fixed bottom-4 left-1/2 -translate-x-1/2 z-50 flex items-center gap-3 px-4 py-3 rounded-2xl shadow-md"
      style={{ background: '#FFF0E8', border: '1px solid #E5D5CC' }}
    >
      <span className="text-sm text-[#171717]">Ny version tillgänglig</span>
      <button
        onClick={handleUpdate}
        disabled={updating}
        className={`rounded-xl px-4 py-2 text-sm font-semibold text-white min-h-[44px] transition-opacity ${
          updating ? 'opacity-50 cursor-not-allowed' : ''
        }`}
        style={{ background: '#F9A87A' }}
      >
        Uppdatera nu
      </button>
    </div>
  );
}
```
Key requirements per UI-SPEC (D-13, D-14, D-15):
- Non-modal, non-blocking, does not auto-dismiss
- `postMessage({ type: 'SKIP_WAITING' })` to communicate to SW context (NOT calling skipWaiting directly)
- Button disabled + opacity-50 during update (loading state per UI-SPEC interaction table)
- Fixed bottom-center positioning
- Accent CTA: `#F9A87A` background, white text

**2c — Mount in layout.tsx:** Import and add both components to the RootLayout `<body>` element, immediately after the opening `<body>` tag and before `<AuthProvider>`. Final body structure:
```tsx
<body className="min-h-full flex flex-col">
  <OfflineBanner />
  <AuthProvider>{children}</AuthProvider>
  <UpdateToast />
</body>
```
`OfflineBanner` at top (sticky), `UpdateToast` at bottom (fixed). Add the two imports:
```typescript
import { OfflineBanner } from "@/components/OfflineBanner";
import { UpdateToast } from "@/components/UpdateToast";
```
  </action>
  <verify>
    <automated>cd /Users/spangen87/Documents/GitHub/wishlist && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/OfflineBanner.tsx` exists and contains `export function OfflineBanner`
    - `OfflineBanner.tsx` contains `role="status"` and `aria-live="polite"`
    - `OfflineBanner.tsx` contains `window.addEventListener('offline'` and `window.addEventListener('online'`
    - `src/components/UpdateToast.tsx` exists and contains `export function UpdateToast`
    - `UpdateToast.tsx` contains `postMessage({ type: 'SKIP_WAITING' })`
    - `UpdateToast.tsx` contains `fixed bottom-4 left-1/2 -translate-x-1/2`
    - `layout.tsx` contains `import { OfflineBanner }` and `import { UpdateToast }`
    - `layout.tsx` contains `<OfflineBanner />` and `<UpdateToast />`
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>OfflineBanner and UpdateToast are created, TypeScript-clean, and mounted in the root layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Worker ↔ Page | SW receives postMessage from page; must only act on trusted message types |
| navigator.onLine ↔ UI | Browser API not fully reliable — banner is a progressive enhancement only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | UpdateToast `postMessage` | accept | Message type `SKIP_WAITING` is a standard SW pattern; no sensitive data transmitted; SW only calls `self.skipWaiting()` which is benign |
| T-05-01-02 | Information Disclosure | layout.tsx OpenGraph metadata | accept | Metadata is intentionally public; no PII or secrets in title/description |
| T-05-01-03 | Denial of Service | OfflineBanner DOM manipulation | accept | Pure client-side toggle; no server calls; cannot affect server state |
| T-05-01-04 | Spoofing | CSS custom property injection | accept | globals.css is a build artifact; no runtime CSS injection; CSP not affected by static CSS |
</threat_model>

<verification>
After both tasks complete:

1. Run TypeScript check: `npx tsc --noEmit` — must exit 0
2. Verify accent token: `grep "color-accent" src/app/globals.css` — must show `#F9A87A` and `#F49060`, not `#F97316` or `#EA6C0A`
3. Verify metadata: `grep -n "önskelista\|lang=" src/app/layout.tsx` — must show Swedish strings and `lang="sv"`
4. Verify component mounts: `grep "OfflineBanner\|UpdateToast" src/app/layout.tsx` — must show both imports and JSX usage
5. Verify offline page: `test -f src/app/offline/page.tsx && grep "Du är offline" src/app/offline/page.tsx`
</verification>

<success_criteria>
- `--color-accent: #F9A87A` and `--color-accent-hover: #F49060` in globals.css (vivid orange gone)
- Root layout has title "Min önskelista", description and OpenGraph in Swedish, lang="sv"
- OfflineBanner: sticky top, offline/online event listeners, fade transition, role="status" aria-live="polite"
- UpdateToast: fixed bottom-center, postMessage SKIP_WAITING, non-blocking, non-auto-dismiss
- Both components mounted in root layout (OfflineBanner before AuthProvider, UpdateToast after)
- TypeScript clean (`npx tsc --noEmit` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/05-pwa-polish/05-01-SUMMARY.md`
</output>
